#ifndef EASYFIND_HPP
# define EASYFIND_HPP

template<typename T>
int easyfind(T *array, int n)
{
	try
	{
		/* code */
	}
	catch(const std::exception& e)
	{
		std::cerr << e.what() << '\n';
	}
	
}

#endif